# Graph

Phase 1 placeholder. This folder belongs to the pure C# domain/algorithm layer and must not depend on Blazor UI components.


Before implementing Graphs, reuse `DataStructures/Matrix` for adjacency-matrix storage/visual learning rather than creating a second matrix implementation. The Graph module should then add its own adjacency-list representation and traversal logic on top of the shared simulation architecture.
