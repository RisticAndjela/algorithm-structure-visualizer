# Backpropagation

Phase 2 step 12. `BackpropagationSimulation` performs a complete forward pass through the same small one-hidden-layer network, computes MSE loss, then applies the chain rule explicitly: output delta, output parameter gradients, hidden deltas, hidden parameter gradients, and one parameter update.

There is no automatic differentiation or neural-network framework hidden behind the visualization.
