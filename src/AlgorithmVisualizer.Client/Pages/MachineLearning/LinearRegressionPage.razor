@page "/ml-foundations/linear-regression"

<PageTitle>Linear Regression — Machine Learning — Algorithm Visualizer</PageTitle>

<LearningPlaceholder
    Eyebrow="MACHINE LEARNING · 02"
    Title="Linear Regression"
    Description="The next lesson will fit a line to examples, measure prediction error and reuse the live Gradient Descent optimizer to improve the model."
    Complexity="Θ(k · m · n)"
    ComplexityNote="planned: k training steps, m examples, n input values"
    Concepts="prediction · error · mean squared error · weights · Gradient Descent"
    LearnTitle="Fit a line by making prediction error smaller."
    LearnSummary="Keep the idea simple: make a prediction, compare it with the real answer, measure the error, then use Gradient Descent to improve the parameters."
    LearnCards="PREDICT|Make one estimate|The model combines input values with weights to produce a number.;;COMPARE|Check prediction vs target|The difference tells us how wrong this example is.;;MEASURE|Combine errors into one loss|Mean squared error summarizes the training error.;;IMPROVE|Reuse Gradient Descent|The model provides the loss and gradient; the existing optimizer moves the parameters."
    ConceptHref="/learn/concepts/linear-regression"
    TipTitle="Separate the model from the optimizer."
    TipText="Linear Regression should explain prediction and error. Gradient Descent should remain the reusable part that updates parameters."
    FocusText="Show prediction → error → loss → update as separate visible steps."
    PromptText="Which step measures the model, and which step changes it?" />
