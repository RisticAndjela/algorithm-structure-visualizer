@page "/ml-foundations/linear-regression"

<PageTitle>Linear Regression — AI/ML Foundations — Algorithm Visualizer</PageTitle>

<LearningPlaceholder
    Eyebrow="AI / ML PHASE 1"
    Title="Linear Regression"
    Description="The next model will combine feature vectors, prediction error and the live Gradient Descent foundation to learn weights instead of duplicating optimizer logic."
    Complexity="Θ(k · m · n)"
    ComplexityNote="planned training loop: k updates, m samples, n features"
    Concepts="prediction · residual · mean squared error · weights · bias · Gradient Descent"
    LearnTitle="Fit a line by minimizing prediction error."
    LearnSummary="Linear Regression will be the first supervised model: compute predictions from vectors, measure residuals, then reuse Gradient Descent to move the weights."
    LearnCards="PREDICT|ŷ = w·x + b|A feature vector and parameter vector meet in a dot product.;;ERROR|Residual = prediction − target|Each sample tells us how wrong the current model is.;;LOSS|MSE aggregates errors|The training objective summarizes many residuals into one score.;;TRAIN|Reuse Gradient Descent|The model supplies the gradient; the existing optimizer pattern moves parameters."
    ConceptHref="/learn/concepts/linear-regression"
    TipTitle="Do not hide the optimizer inside the model."
    TipText="The Linear Regression lesson should expose model-specific prediction and gradient logic while reusing the Vector and Gradient Descent foundations already learned."
    FocusText="Keep prediction, residual, loss and parameter update visually separate so training is understandable rather than a single opaque Fit() call."
    PromptText="Which part belongs to the model, and which part belongs to the optimizer?" />
