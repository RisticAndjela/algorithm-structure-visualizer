@* Legacy compatibility stub.
   Binary Heap no longer owns a separate page or implementation.
   /structures/binary-heap is intentionally registered only by GeneralHeapPage,
   where Binary Heap is represented as the d = 2 case of the shared d-ary heap lab.

   This file remains non-routable so patch archives can safely overlay older
   working copies where HeapPage.razor still exists. *@
